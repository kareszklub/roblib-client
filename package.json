{
    "name": "@kareszklub/roblib-client",
    "version": "2.0.0-alpha.1",
    "description": "A remote library for a dank engine.",
    "license": "LGPL-3.0",
    "type": "module",
    "main": "out/client-node.js",
    "engines": {
        "node": ">=14.8"
    },
    "dependencies": {
        "ws": "^8.4.0",
        "@types/node": "^17.0.6",
        "@types/ws": "^8.2.2",
        "typescript": "^4.5.4"
    },
    "devDependencies": {
        "esbuild": "^0.14.10"
    },
    "scripts": {
        "check": "tsc --noEmit",
        "postinstall": "tsc",
        "bundle": "esbuild src/client-web.ts --platform=browser --bundle --minify --outdir=out",
        "clean": "node -e \"require('fs').rmSync('out',{recursive:true})\""
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/kareszklub/roblib-client.git"
    }
}
